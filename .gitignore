# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*
!.env.example

supabase
supabase-project
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.next/
/.next/

# Backups
db_backups/

# MCP config (contains DB password)
.mcp.json

# Claude Code local settings (machine-specific, may contain MCP credentials)
.claude/settings.local.json

# Third-party skill repo clones (kullanıcı /plugin marketplace ile çekti, repo'ya commit etmiyoruz)
claude-skills/
.tmp/
.tmp-claude-skills/
tmp/

# Live Share local config (machine-specific port mapping)
.vsls.json

# Certificates / private keys (never commit)
*.pem
*.key
*.p12
*.pfx

# TypeScript build artifacts
tsconfig.tsbuildinfo
*.tsbuildinfo
